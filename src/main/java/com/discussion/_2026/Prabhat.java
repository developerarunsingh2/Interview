package com.discussion._2026;

public class Prabhat {
    /*
    * 14-06*/

    /*how to limit number of transactions in banking application? ans: bucket4j
    how internally @EnableAutoConfiguration work? @InjectSelector used to select classes
            what happens if producer data keeps on sending message and consumer not consuming it? how you can
    resolve this issue ? ans: increasing limit of heartbeat of consumer
            in @Transactional*/

}
